#include <bits/stdc++.h>
using namespace std;
int main(int argc, char *argv[])
{
    ios::sync_with_stdio(0), cin.tie(0);
    long n = 0, q = 0, m = 0, ask = 0;
    cin >> n >> q;
    long datas[n];
    for (long i = 0; i < n; i++)
    {
        cin >> datas[i];
    }
    while (q--)
    {
        long l = 1, r = n;
        m = (r + l) / 2;
        cin >> ask;
        while (r - l)
        {
            if(ask>)
        }
    }
    return 0;
}