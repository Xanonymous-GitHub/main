#include<bits/stdc++.h>
using namespace std;
int qs(int* data){
    
}
int main(int argc,char* argv[]){
    ios::sync_with_stdio(0),cin.tie(0);
    int n=0;
    cin>>n;
    vector<int> data(n);
    for (int i = 0; i < n; i++)cin>>data[i];
    
    return 0;
}