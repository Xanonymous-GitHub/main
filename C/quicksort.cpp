#include<bits/stdc++.h>
using namespace std;
int quicksort(vector<int> data){
    
    return 0;
}
int main(int argc,char* argv[]){
    ios::sync_with_stdio(0),cin.tie(0);
    //get user data
    int n=0;
    cin>>n;
    vector<int> data(n);
    for (int i = 0; i < n; i++)cin>>data[i];
    quicksort(data);
    return 0;
}